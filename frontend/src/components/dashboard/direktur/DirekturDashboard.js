"use client";

import Dashboard from "@/components/dashboard/reviewer/Dashboard";

export default function DirekturDashboard() {
  // Direktur can see all reports, so no filter is applied
  const filterDashboard = () => true;
}
